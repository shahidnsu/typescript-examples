let skill: [string, number]
skill = ['progrming', 5]
console.log(skill)


//optional tuples parameter

let bgColor, headColor: [number, number, number, number?]


bgColor = [0, 255, 255, 0.5]
headColor = [0, 255, 255]
console.log(bgColor)
console.log(headColor)
